// OWNER: webmcp+product agent. Reads /p/<host>/<path>, fetches /api/extract, renders the product, registers the 8 tools.
export function ProductPage() {
  return <main className="container"><h1>Product</h1><p>Product page: not built yet.</p></main>;
}
