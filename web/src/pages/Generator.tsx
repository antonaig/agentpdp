// OWNER: panel+generator agent. Landing page: paste a PDP URL → navigate to /p/<host>/<path>. Registers the `make_agent_ready` tool.
export function GeneratorPage() {
  return <main className="container"><h1>AgentPDP</h1><p>Generator page: not built yet.</p></main>;
}
